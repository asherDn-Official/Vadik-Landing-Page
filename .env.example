# Server Configuration
PORT=5000

# MongoDB Connection (Replace with your actual MongoDB URI in .env)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/vadik-ai?retryWrites=true&w=majority

# Environment
NODE_ENV=development

# Note: Create a .env file with your actual values
# Do not commit the .env file to version control